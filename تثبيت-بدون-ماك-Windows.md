# تثبيت صلاتي على الآيفون من Windows (بدون Mac)

> عندك PC + آيفون فقط - هذه الطريقة الكاملة

## الخطوة 1: ارفع المشروع لـ GitHub (5 دقائق)

1. ادخل https://github.com/new وسجل حساب مجاني
2. سمّ المشروع `salati` واختر **Public** واضغط Create
3. في الـ PC افتح مجلد `C:\Users\DELL\Desktop\صلاتي`
4. اضغط كليك يمين > **Open in Terminal** واكتب:
```
git init
git add .
git commit -m "صلاتي"
git branch -M main
git remote add origin https://github.com/اسمك/salati.git
git push -u origin main
```
(بدّل `اسمك` باسمك في GitHub)

> لو ما عندك git نزّله من https://git-scm.com/download/win

## الخطوة 2: بناء IPA تلقائياً (مجاناً)

بعد الرفع، ادخل على GitHub > تبويب **Actions** > بتشوف `Build iOS IPA` شغال
- انتظر 5-8 دقائق
- ادخل على آخر Run > تحت **Artifacts** حمّل `Salati-IPA` > فك الضغط بتحصل `Salati.ipa`

> كل ما تعدل في التطبيق وترفع push جديد، يبني IPA تلقائياً

## الخطوة 3: ثبّت على الآيفون بـ Sideloadly (من Windows)

1. نزّل **Sideloadly** من https://sideloadly.io (مجاني)
2. نزّل **iTunes** من https://www.apple.com/itunes (ضروري)
3. وصّل الآيفون بالـ USB وافتح **Sideloadly**
4. في Sideloadly:
   - اختر آيفونك من القائمة
   - اسحب ملف `Salati.ipa` إلى المربع
   - اكتب **Apple ID** وكلمة السر (حساب عادي مجاني يكفي)
   - اضغط **Start**
5. لو طلب كود تحقق، انسخه من الآيفون
6. انتظر حتى يقول **Done**

## الخطوة 4: وثّق التطبيق في الآيفون

1. الآيفون > الإعدادات > عام > **إدارة الجهاز VPN** (أو Device Management)
2. اضغط على Apple ID > **وثّق**
3. ارجع للشاشة الرئيسية > افتح 🕌 **صلاتي**

## ملاحظات مهمة
- الحساب المجاني: التطبيق يشتغل 7 أيام فقط، بعدها أعد الخطوة 3 فقط (بدون رفع GitHub)
- الحساب المدفوع ($99): سنة كاملة + TestFlight
- كل مرة تفتح التطبيق أول مرة اسمح بـ **الموقع** و **الإشعارات** عشان القبلة والأذان

## الطريقة السريعة المؤقتة (بدون أي تثبيت)
افتح `معاينة-التطبيق.html` في Safari > مشاركة > **إضافة إلى الشاشة الرئيسية** - يشتغل فوراً كتطبيق

## مشاكل شائعة
- **Sideloadly لا يرى الآيفون**: ثبّت iTunes وافعل Trust على الآيفون
- **Failed to build**: تأكد رفعت كل الملفات في `Salati/` و `Salati.xcodeproj/`
- **Unable to verify**: أعد الوثوق من الإعدادات

تحتاج مساعدة في الرفع؟ أرسل لي اسم GitHub وأجهز لك الأوامر الجاهزة.
